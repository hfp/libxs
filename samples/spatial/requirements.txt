# Optional Python dependencies for the spatial samples.
# The default synthetic dense3d sample uses only the Python standard library.
numpy>=1.20
h5py>=3.0
# Pair-counting validation (requires libgsl-dev at build time)
corrfunc>=2.5